machine:
  timezone:
    Australia/Sydney
  node:
    version: 6
dependencies:
  override:
    - sh ./deps.sh
  cache_directories:
    - "node_modules"
test:
  override:
    - sh ./test.sh
general:
  artifacts:
    - "page.png"
