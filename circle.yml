machine:
  timezone:
    Australia/Sydney
  node:
    version: node
dependencies:
  override:
    - sh ./deps.sh
  cache_directories:
    - "node_modules"
test:
  override:
    - sh ./test.sh
general:
  artifacts:
    - "page.png"
deployment:
  production:
    branch: master
    commands:
      - sh ./deploy.sh
