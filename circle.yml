machine:
  timezone:
    Australia/Sydney
  node:
    version: node
dependencies:
  override:
    - sh ./deps.sh
  cache_directories:
    - "node_modules"
test:
  override:
    - sh ./test.sh
general:
  artifacts:
    - "page.png"
deployment:
  production:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
      - sh ./deploy.sh
