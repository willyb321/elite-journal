machine:
  timezone:
    Australia/Sydney
  node:
    version: node
dependencies:
  override:
    - sh ./deps.sh
  cache_directories:
    - "node_modules"
    - "~/.apt-cache"
test:
  override:
    - sh ./test.sh
general:
  artifacts:
    - "page.png"
