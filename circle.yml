machine:
  timezone:
    Australia/Sydney
  node:
    version: node
dependencies:
  pre:
    - sh ./.scripts/yarninstall.sh
  override:
    - sh ./.scripts/deps.sh
  cache_directories:
    - "node_modules"
    - "~/.yarn-cache"
general:
  artifacts:
    - "page.png"
