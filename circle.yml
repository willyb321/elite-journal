machine:
  timezone:
    Australia/Sydney
  node:
    version: node
dependencies:
  pre:
    - sh ./.scripts/yarninstall.sh
  override:
    - sh ./.scripts/deps.sh
  cache_directories:
    - "node_modules"
    - "~/.apt-cache"
test:
  override:
    - sh ./.scripts/test.sh
general:
  artifacts:
    - "page.png"
